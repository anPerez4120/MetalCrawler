<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name = "android:textAppearanceButton">@font/dpcomic</item>
        <!--item name = "android:textAppearanceListItem">@font/dpcomic</item-->
        <item name = "android:textAppearance">@font/dpcomic</item>
        <item name = "android:fontFamily">@font/dpcomic</item>
        <item name="android:windowBackground">@color/colorPrimary</item>

        <item name="android:textColor">@color/colorAccent</item>

        <item name="android:windowActionBar">@color/colorAccent</item>
        <item name="android:windowActionBarOverlay">@color/colorAccent</item>

        <item name="android:colorButtonNormal">@color/colorHighscore</item>

    </style>

</resources>
